import type { Metadata } from "next";
import Hero from "@/components/Hero";
import SubToolContent from "@/components/SubToolContent";

const siteUrl = "https://www.text-cleaner-ai.com";

export const metadata: Metadata = {
  title: "Clean Claude Code Output — Strip Markdown, Backticks & CLI Artifacts",
  description:
    "Clean text generated by Claude Code (Anthropic's CLI agent). Strip markdown asterisks, code backticks, smart quotes, and hidden unicode from terminal output before pasting into docs, commits, or PRs.",
  keywords: [
    "clean claude code output",
    "claude code clean text",
    "claude code /clean",
    "strip claude code markdown",
    "claude code output cleaner",
    "clean ai cli output",
  ],
  alternates: {
    canonical: "/clean-claude-code",
  },
  openGraph: {
    title: "Clean Claude Code Output — Strip Markdown, Backticks & CLI Artifacts",
    description:
      "Paste terminal or chat output from Claude Code to instantly strip markdown formatting, backticks, and hidden unicode artifacts.",
    url: `${siteUrl}/clean-claude-code`,
  },
};

const removedItems = [
  {
    character: "Markdown Bold/Italic Asterisks",
    unicode: "U+002A (*)",
    description: "Claude Code wraps file names, flags, and emphasis in **bold** or *italic* markdown that renders as raw asterisks outside a terminal or chat pane.",
  },
  {
    character: "Inline & Block Code Backticks",
    unicode: "U+0060 (`)",
    description: "Single and triple backticks around commands, paths, and code snippets that need to be stripped for plain-text commit messages or PR descriptions.",
  },
  {
    character: "Smart Quotes & Em Dashes",
    unicode: "U+201C / U+201D / U+2014",
    description: "Curly quotes and em dashes that break shell scripts, JSON configs, or plain-text files when pasted verbatim.",
  },
  {
    character: "Invisible Unicode Spacers",
    unicode: "U+200B / U+00A0",
    description: "Zero-width spaces and non-breaking spaces that sometimes appear between rendered response paragraphs and can break regex or diff tooling.",
  },
];

const howToSteps = [
  {
    title: "Copy Claude Code's Response",
    description: "Copy the markdown-formatted reply Claude Code printed in your terminal, IDE panel, or chat transcript.",
  },
  {
    title: "Paste into Cleaner",
    description: "Paste into the editor above. Our engine strips markdown, backticks, and hidden unicode by default.",
  },
  {
    title: "Copy Clean Plain Text",
    description: "Copy the sanitized output for commit messages, PR descriptions, docs, or plain-text files.",
  },
];

const faqs = [
  {
    question: "Does Claude Code have a built-in /clean command?",
    answer:
      "Not currently — Claude Code doesn't ship a native /clean slash command for stripping markdown from its own output. This tool is a free browser-based equivalent: paste any Claude Code response and get plain text back instantly.",
  },
  {
    question: "Why does Claude Code's output have asterisks and backticks in it?",
    answer:
      "Claude Code formats responses in Markdown — bold file names, bulleted steps, and backtick-wrapped commands render cleanly in a terminal or IDE, but the raw symbols stay behind when you copy that text into a commit message, README, or plain-text file.",
  },
  {
    question: "Is my code or terminal output uploaded anywhere?",
    answer:
      "No. All cleaning runs 100% locally in your browser's JavaScript runtime. Nothing is sent to a server, which matters if your Claude Code output includes proprietary code or file paths.",
  },
  {
    question: "Will this remove code content, or just formatting?",
    answer:
      "Only formatting artifacts — markdown symbols, smart quotes, and invisible unicode. Your actual code, commands, and text content are preserved exactly as written.",
  },
  {
    question: "Can I use this for commit messages and PR descriptions?",
    answer:
      "Yes — that's the main use case. Paste Claude Code's summary of a change, strip the markdown asterisks and backticks, and get a clean plain-text commit message or PR body.",
  },
];

const webAppJsonLd = {
  "@context": "https://schema.org",
  "@type": "WebApplication",
  name: "Claude Code Output Cleaner",
  url: `${siteUrl}/clean-claude-code`,
  description: "Free tool to strip markdown formatting and hidden unicode from Claude Code CLI output.",
  applicationCategory: "UtilitiesApplication",
  operatingSystem: "Any",
  offers: {
    "@type": "Offer",
    price: "0",
    priceCurrency: "USD",
  },
};

const howToJsonLd = {
  "@context": "https://schema.org",
  "@type": "HowTo",
  name: "How to Clean Claude Code Output",
  step: howToSteps.map((step) => ({
    "@type": "HowToStep",
    name: step.title,
    text: step.description,
  })),
};

const faqJsonLd = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  mainEntity: faqs.map((faq) => ({
    "@type": "Question",
    name: faq.question,
    acceptedAnswer: {
      "@type": "Answer",
      text: faq.answer,
    },
  })),
};

export default function CleanClaudeCodePage() {
  return (
    <>
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(webAppJsonLd) }}
      />
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(howToJsonLd) }}
      />
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(faqJsonLd) }}
      />
      <Hero
        heading="Clean Claude Code Output"
        subheading="Strip markdown asterisks, code backticks, smart quotes, and hidden unicode from Claude Code's terminal output before it goes into a commit, PR, or doc."
        initialOptions={{
          removeMarkdown: true,
          normalizeQuotes: true,
          normalizeDashes: true,
          removeTrailingWhitespace: true,
          removeHiddenCharacters: true,
        }}
      />
      <SubToolContent
        title="Clean Claude Code Output"
        directAnswerTitle="How to Clean Claude Code's Output for Commits, PRs, and Docs"
        directAnswerText="Claude Code — Anthropic's command-line coding agent — formats its responses in Markdown: bold file names, bulleted steps, and backtick-wrapped commands. That renders fine in a terminal or IDE, but paste it into a commit message, README, or plain-text file and the raw asterisks, backticks, and smart quotes come along too. This tool strips those artifacts in your browser, leaving clean plain text with your content untouched."
        beforeExample={"**Summary:**\n- Updated `src/lib/cleanText.ts` to fix the *normalizeDashes* bug.\n- Ran tests — all “green”."}
        afterExample={'Summary:\n- Updated src/lib/cleanText.ts to fix the normalizeDashes bug.\n- Ran tests - all "green".'}
        removedItems={removedItems}
        howToSteps={howToSteps}
        faqs={faqs}
      />
    </>
  );
}
